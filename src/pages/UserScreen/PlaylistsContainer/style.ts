import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  playlistsContainer: {
    borderColor: '#f00',
    borderWidth: 0,

    alignItems: 'center'
  },
  typeOfPlaylist: {
    borderColor: '#f00',
    borderWidth: 0,

    fontSize: 20,
    color: '#fff',
  }
});

export default styles;